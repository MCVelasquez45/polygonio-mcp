# AI-Trader — Service Dependency Graph

> Generated by `npm run graph`. Do not edit by hand.

Solid nodes are enabled services; dashed nodes are disabled placeholders.

```mermaid
flowchart TD
  subgraph mcp["mcp"]
    python_mcp["Python MCP (Agent)<br/>:5001"]
  end
  subgraph analytics["analytics"]
    screener["Options Screener<br/>:8001"]
  end
  subgraph backend["backend"]
    backend["Backend API (Gateway)<br/>:4000"]
  end
  subgraph frontend["frontend"]
    frontend["Frontend (Vite)<br/>:5173"]
  end
  subgraph worker["worker"]
    options_worker("Options Worker")
    scheduler("Scheduler")
  end
  subgraph realtime["realtime"]
    websocket_gateway("WebSocket Gateway<br/>:4100")
  end
  python_mcp --> backend
  backend --> frontend
  backend --> options_worker
  backend --> websocket_gateway
  backend --> scheduler
  classDef disabled fill:#eee,stroke:#bbb,color:#999,stroke-dasharray:4 3;
  class options_worker,websocket_gateway,scheduler disabled;
```

## Startup order (parallel waves)

0. Python MCP (Agent), Options Screener
1. Backend API (Gateway)
2. Frontend (Vite)
