# Analysis Feature

Hosts AI-driven research endpoints:
- `analysis.routes.ts`: `/api/analysis` endpoints for watchlist reports and the options checklist cache.
- Relies on services from `../options` for data crunching.
