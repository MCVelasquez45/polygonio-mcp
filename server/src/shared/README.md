# Shared Utilities

Cross-feature helpers live here:
- `db/mongo.ts` – MongoDB connection + collection helper.
- `data/massive.ts` – Massive.com API client and rate-limited request queue.

Additional shared utilities should be grouped into subfolders (e.g., `auth`, `http`).
